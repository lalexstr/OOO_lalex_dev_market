# Backend Microservices Architecture

Микросервисная архитектура бэкенда на Go, состоящая из 6 независимых сервисов, каждый из которых отвечает за свою бизнес-логику.

## 📋 Содержание

- [Обзор](#обзор)
- [Архитектура](#архитектура)
- [Технологии](#технологии)
- [Сервисы](#сервисы)
- [Быстрый старт](#быстрый-старт)
- [API Документация](#api-документация)
- [Разработка](#разработка)
- [Docker](#docker)
- [Переменные окружения](#переменные-окружения)

---

## 🎯 Обзор

Система построена на микросервисной архитектуре, где каждый сервис:
- Имеет свою базу данных (SQLite)
- Работает на отдельном порту
- Может быть развернут независимо
- Взаимодействует с другими сервисами через HTTP API

### Основные возможности

- ✅ Аутентификация и авторизация (JWT)
- ✅ Управление пользователями с ролями (admin/user)
- ✅ Управление продуктами
- ✅ Управление проектами с отслеживанием прогресса
- ✅ Управление портфолио
- ✅ Обработка контактных запросов
- ✅ Логирование действий пользователей
- ✅ Внутренние API для межсервисного взаимодействия

---

## 🏗️ Архитектура

```
                    ┌─────────────────────┐
                    │   Auth Service      │ ← Центральный сервис аутентификации
                    │   (Port 8080)       │   (JWT токены, роли пользователей)
                    └──────────┬──────────┘
                               │
              ┌────────────────┼────────────────┐
              │                │                │
    ┌─────────▼─────────┐ ┌───▼────┐ ┌─────────▼─────────┐
    │ Product Service   │ │ Project│ │  User Service     │
    │   (Port 8081)     │ │ Service│ │   (Port 8085)      │
    │ ✅ Middleware     │ │ :8082  │ │ ✅ Middleware      │
    │ ✅ Auth Client    │ │ ✅ Both│ │ ✅ Auth Client     │
    └───────────────────┘ └────────┘ └────────────────────┘
              │                │                │
              └────────────────┼────────────────┘
                               │
              ┌────────────────┼────────────────┐
              │                │                │
    ┌─────────▼─────────┐ ┌───▼────┐
    │ Portfolio Service │ │ Contact │
    │   (Port 8083)     │ │ Service │
    │ ✅ Middleware     │ │ :8084   │
    │ ✅ Auth Client    │ │ ✅ Both │
    └───────────────────┘ └────────┘
```

### Принципы архитектуры

1. **Разделение ответственности** - каждый сервис отвечает за свою доменную область
2. **Независимость** - сервисы могут быть развернуты и масштабированы независимо
3. **Централизованная аутентификация** - Auth Service является единой точкой аутентификации
4. **Межсервисная коммуникация** - через HTTP REST API
5. **Изолированные базы данных** - каждый сервис имеет свою БД

---

## 🛠️ Технологии

### Основной стек

- **Go 1.21+** - язык программирования
- **Gin** - HTTP веб-фреймворк
- **GORM** - ORM для работы с базами данных
- **SQLite** - база данных (по одному файлу на сервис)
- **JWT** (golang-jwt/jwt) - токены для аутентификации
- **bcrypt** - хеширование паролей

### Дополнительные инструменты

- **Docker** - контейнеризация
- **Make** - автоматизация задач
- **godotenv** - управление переменными окружения

---

## 🚀 Сервисы

### 1. Auth Service (Порт 8080)

**Центральный сервис аутентификации и управления пользователями**

- Регистрация и вход пользователей
- Выдача JWT токенов
- Управление пользователями (CRUD)
- Роли: `admin` и `user`
- Внутренние API для других сервисов

**База данных:** `auth.db`

**Основные эндпоинты:**
- `POST /api/v1/register` - регистрация
- `POST /api/v1/login` - вход
- `GET /api/v1/me` - текущий пользователь
- `GET /api/v1/users` - список пользователей (admin)
- `GET /api/v1/internal/users/:id` - внутренний API

---

### 2. Product Service (Порт 8081)

**Управление продуктами**

- CRUD операции с продуктами
- Публичный список продуктов
- Админская панель для управления
- Логирование действий

**База данных:** `product.db`

**Основные эндпоинты:**
- `GET /api/products/public` - публичный список
- `POST /api/products` - создать продукт (admin)
- `GET /api/products` - список продуктов (admin)
- `PATCH /api/products/:id` - обновить продукт (admin)
- `DELETE /api/products/:id` - удалить продукт (admin)

**Зависимости:** Auth Service (для проверки JWT и роли)

---

### 3. Project Service (Порт 8082)

**Управление проектами и отслеживание прогресса**

- Создание и управление проектами
- Отслеживание прогресса (0-100%)
- Статусы проектов
- Персональные проекты пользователей
- Сводка по проектам

**База данных:** `project.db`

**Основные эндпоинты:**
- `POST /api/projects` - создать проект (admin)
- `GET /api/projects` - список всех проектов (admin)
- `PATCH /api/projects/:id` - обновить проект (admin)
- `PATCH /api/projects/:id/progress` - обновить прогресс (admin)
- `GET /api/me/projects` - мои проекты
- `GET /api/me/projects/summary` - сводка по проектам

**Зависимости:** Auth Service (для проверки JWT и роли)

---

### 4. Portfolio Service (Порт 8083)

**Управление портфолио**

- CRUD операции с портфолио
- Публичный просмотр списка портфолио
- Админская панель для управления (создание, обновление, удаление)
- Логирование операций

**База данных:** `portfolio.db`

**Основные эндпоинты:**
- `GET /api/portfolio` - список портфолио (публичный)
- `POST /api/portfolio` - создать портфолио (admin)
- `PATCH /api/portfolio/:id` - обновить портфолио (admin)
- `DELETE /api/portfolio/:id` - удалить портфолио (admin)

**Зависимости:** Auth Service (для проверки JWT и роли)

---

### 5. Contact Service (Порт 8084)

**Обработка контактных запросов**

- Создание контактных запросов (публичный доступ)
- Управление статусами запросов (только для админов)
- Админская панель с аутентификацией
- Логирование действий

**База данных:** `contact.db`

**Основные эндпоинты:**
- `POST /api/contact-requests` - создать запрос (публичный)
- `GET /api/contact-requests/admin` - список запросов (admin)
- `PATCH /api/contact-requests/admin/:id/status` - обновить статус (admin)
- `DELETE /api/contact-requests/admin/:id` - удалить запрос (admin)
- `GET /api/logs` - получить логи

**Зависимости:** Auth Service (для проверки JWT и роли)

**Статусы:** `new`, `processed`, `rejected`

---

### 6. User Service (Порт 8085)

**Расширенное управление пользователями и аналитика**

- Список пользователей с фильтрацией
- Изменение ролей пользователей
- Просмотр активности пользователей
- Логирование действий администраторов

**База данных:** `user.db`

**Основные эндпоинты:**
- `GET /api/users` - список пользователей с фильтрами (admin)
- `PATCH /api/users/:id/role` - изменить роль (admin)
- `GET /api/users/:id/activity` - активность пользователя (admin)

**Зависимости:** Auth Service (для получения данных пользователей)

---

## 🚀 Быстрый старт

### Предварительные требования

- Go 1.21 или выше
- Make (опционально, для удобства)

### Установка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd ooo"lalex"
```

2. **Настройте переменные окружения:**

Создайте файл `.env` в корне проекта или в каждом сервисе:

```bash
# Общие настройки
JWT_SECRET=your-secret-key-here-change-in-production
AUTH_SERVICE_URL=http://localhost:8080

# Порты (опционально, есть значения по умолчанию)
PORT=8080  # auth-service
PORT=8081  # product-service
PORT=8082  # project-service
PORT=8083  # portfolio-service
PORT=8084  # contact-service
PORT=8085  # user-service
```

3. **Запустите все сервисы:**

**Вариант 1: Используя Make (рекомендуется)**
```bash
make dev
```

**Вариант 2: Используя скрипт**
```bash
./scripts/dev.sh
```

**Вариант 3: Запуск каждого сервиса отдельно**

В разных терминалах:
```bash
# Терминал 1
cd auth-service && go run main.go

# Терминал 2
cd product-service && go run main.go

# Терминал 3
cd project-service && go run main.go

# Терминал 4
cd portfolio-service && go run main.go

# Терминал 5
cd contact-service && go run cmd/main.go

# Терминал 6
cd user-service && go run main.go
```

### Проверка работы

После запуска все сервисы будут доступны:

- Auth Service: http://localhost:8080
- Product Service: http://localhost:8081
- Project Service: http://localhost:8082
- Portfolio Service: http://localhost:8083
- Contact Service: http://localhost:8084
- User Service: http://localhost:8085

### Первый пользователь

При первом запуске Auth Service автоматически создает администратора:
- Email: `admin@example.com`
- Password: `admin123`

---

## 📚 API Документация

### Аутентификация

Большинство эндпоинтов требуют JWT токен в заголовке:
```
Authorization: Bearer <your-jwt-token>
```

#### Получение токена

```bash
curl -X POST http://localhost:8080/api/v1/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "password": "admin123"
  }'
```

Ответ:
```json
{
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

### Примеры запросов

#### Регистрация нового пользователя

```bash
curl -X POST http://localhost:8080/api/v1/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "password123",
    "full_name": "John Doe"
  }'
```

#### Создание продукта (требует admin токен)

```bash
curl -X POST http://localhost:8081/api/products \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Product Name",
    "description": "Product Description",
    "price": 99.99,
    "image_url": "https://example.com/image.jpg"
  }'
```

#### Создание проекта (требует admin токен)

```bash
curl -X POST http://localhost:8082/api/projects \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": 1,
    "title": "Project Title",
    "description": "Project Description"
  }'
```

#### Создание контактного запроса (публичный)

```bash
curl -X POST http://localhost:8084/api/contact-requests \
  -H "Content-Type: application/json" \
  -d '{
    "contact": "user@example.com"
  }'
```

#### Список контактных запросов (требует admin токен)

```bash
curl -X GET "http://localhost:8084/api/contact-requests/admin?status=new" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

#### Обновление статуса контактного запроса (требует admin токен)

```bash
curl -X PATCH http://localhost:8084/api/contact-requests/admin/1/status \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "status": "processed"
  }'
```

#### Удаление контактного запроса (требует admin токен)

```bash
curl -X DELETE http://localhost:8084/api/contact-requests/admin/1 \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

#### Просмотр портфолио (публичный)

```bash
curl -X GET http://localhost:8083/api/portfolio
```

#### Создание портфолио (требует admin токен)

```bash
curl -X POST http://localhost:8083/api/portfolio \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Portfolio Item",
    "description": "Portfolio Description",
    "image_url": "https://example.com/image.jpg"
  }'
```

#### Обновление портфолио (требует admin токен)

```bash
curl -X PATCH http://localhost:8083/api/portfolio/1 \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Updated Portfolio Item"
  }'
```

#### Удаление портфолио (требует admin токен)

```bash
curl -X DELETE http://localhost:8083/api/portfolio/1 \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

### Полная документация API

Подробная документация по всем эндпоинтам находится в README каждого сервиса:
- [Auth Service README](auth-service/README.md)
- [Product Service README](product-service/README.md)
- [Project Service README](project-service/README.md)
- [Portfolio Service README](portfolio-service/README.md)
- [Contact Service README](contact-service/README.md)
- [User Service README](user-service/README.md)

---

## 💻 Разработка

### Структура проекта

```
.
├── auth-service/          # Сервис аутентификации
│   ├── config/           # Конфигурация
│   ├── db/               # Инициализация БД
│   ├── handlers/         # HTTP обработчики
│   ├── middleware/       # JWT middleware
│   ├── models/           # Модели данных
│   ├── repositories/     # Слой доступа к данным
│   ├── routes/           # Маршруты
│   ├── services/         # Бизнес-логика
│   └── utils/            # Утилиты
├── product-service/      # Сервис продуктов
├── project-service/      # Сервис проектов
├── portfolio-service/   # Сервис портфолио
├── contact-service/     # Сервис контактов
├── user-service/        # Сервис пользователей
├── scripts/             # Скрипты для разработки
├── logs/                # Логи сервисов
└── Makefile             # Команды для разработки
```

### Команды Make

```bash
make help              # Показать справку
make dev               # Запустить все сервисы
make dev-auth          # Запустить только auth-service
make dev-user          # Запустить только user-service
make dev-product       # Запустить только product-service
make dev-project       # Запустить только project-service
make dev-contact       # Запустить только contact-service
make dev-portfolio     # Запустить только portfolio-service
make stop              # Остановить все сервисы
make clean             # Очистить логи и временные файлы
```

### Логи

Логи всех сервисов сохраняются в директории `logs/`:
- `auth-service.log`
- `product-service.log`
- `project-service.log`
- `portfolio-service.log`
- `contact-service.log`
- `user-service.log`

---

## 🐳 Docker

Каждый сервис может быть развернут с помощью Docker. Примеры `docker-compose.yml` находятся в директориях сервисов.

### Запуск через Docker Compose

```bash
# В директории сервиса
cd auth-service
docker-compose up -d
```

### Сборка Docker образов

```bash
# Для каждого сервиса
cd auth-service
docker build -t auth-service .
```

---

## 🔐 Переменные окружения

### Общие переменные

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `JWT_SECRET` | Секретный ключ для JWT | **Обязательно** |
| `AUTH_SERVICE_URL` | URL Auth Service | `http://localhost:8080` |

### Auth Service

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `PORT` | Порт сервера | `8080` |
| `JWT_TTL_MINUTES` | Время жизни токена (минуты) | `60` |
| `SQLITE_PATH` | Путь к БД | `auth.db` |

### Product Service

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `PORT` | Порт сервера | `8081` |
| `DB_PATH` | Путь к БД | `./product.db` |
| `JWT_SECRET` | Секретный ключ JWT | **Обязательно** |
| `AUTH_SERVICE_URL` | URL Auth Service | `http://localhost:8080` |

### Project Service

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `PORT` | Порт сервера | `8082` |
| `DB_PATH` | Путь к БД | `./project.db` |
| `JWT_SECRET` | Секретный ключ JWT | **Обязательно** |
| `AUTH_SERVICE_URL` | URL Auth Service | `http://localhost:8080` |

### Portfolio Service

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `PORT` | Порт сервера | `8083` |
| `DB_PATH` | Путь к БД | `./portfolio.db` |
| `JWT_SECRET` | Секретный ключ JWT | **Обязательно** |
| `AUTH_SERVICE_URL` | URL Auth Service | `http://localhost:8080` |

### Contact Service

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `PORT` | Порт сервера | `8084` |
| `DB_PATH` | Путь к БД | `./contact.db` |
| `LOG_LEVEL` | Уровень логирования | `info` |
| `JWT_SECRET` | Секретный ключ JWT | **Обязательно для admin эндпоинтов** |
| `AUTH_SERVICE_URL` | URL Auth Service | `http://localhost:8080` |

### User Service

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `PORT` | Порт сервера | `8085` |
| `DB_PATH` | Путь к БД | `./user.db` |
| `JWT_SECRET` | Секретный ключ JWT | **Обязательно** |
| `AUTH_SERVICE_URL` | URL Auth Service | `http://localhost:8080` |

---

## 🔄 Межсервисное взаимодействие

### Внутренние API Auth Service

Другие сервисы используют внутренние эндпоинты Auth Service:

- `GET /api/v1/internal/users/:id` - получить пользователя
- `GET /api/v1/internal/users/:id/role` - получить роль пользователя
- `PATCH /api/v1/internal/users/:id/role` - обновить роль

Эти эндпоинты не требуют аутентификации и предназначены для внутреннего использования.

### Пример использования Auth Client

```go
authClient := clients.NewAuthClient()
role, err := authClient.GetUserRole(userID)
if err != nil {
    // обработка ошибки
}
```

---

## 📝 Логирование

Все сервисы логируют важные действия:
- Создание, обновление, удаление ресурсов
- Изменение ролей пользователей
- Обработка контактных запросов
- Обновление прогресса проектов

Логи сохраняются в базах данных каждого сервиса в таблице `logs`.

---

## 🧪 Тестирование

Для тестирования API можно использовать:
- `curl` (примеры в документации)
- Postman
- HTTPie
- Любой другой HTTP клиент

### Пример тестового сценария

1. Регистрация пользователя
2. Вход и получение токена
3. Создание продукта (с токеном)
4. Получение списка продуктов
5. Обновление продукта
6. Удаление продукта

---

## 🚨 Безопасность

### Рекомендации для продакшена

1. **JWT_SECRET** - используйте сильный случайный ключ
2. **HTTPS** - всегда используйте HTTPS в продакшене
3. **CORS** - настройте CORS для фронтенда
4. **Rate Limiting** - добавьте ограничение запросов
5. **Валидация** - все входные данные валидируются
6. **Пароли** - хранятся только как bcrypt хеши
7. **Базы данных** - рассмотрите использование PostgreSQL вместо SQLite для продакшена

---

## 📦 Развертывание

### Продакшен

1. Используйте переменные окружения для конфигурации
2. Настройте reverse proxy (nginx/traefik)
3. Используйте PostgreSQL вместо SQLite
4. Настройте мониторинг и логирование
5. Используйте Docker для контейнеризации
6. Настройте health checks

### Мониторинг

Рекомендуется добавить:
- Health check эндпоинты
- Метрики (Prometheus)
- Логирование (ELK Stack)
- Трейсинг (Jaeger)

---

## 🤝 Вклад

1. Форкните проект
2. Создайте ветку для фичи (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

---

## 📄 Лицензия

MIT License

---

## 👥 Авторы

- Ваше имя

---

## 📞 Контакты

Для вопросов и предложений создайте Issue в репозитории.

---

## 🔗 Полезные ссылки

- [Gin Framework](https://gin-gonic.com/)
- [GORM Documentation](https://gorm.io/)
- [JWT Go Library](https://github.com/golang-jwt/jwt)
- [Go Documentation](https://go.dev/doc/)

---

**Примечание:** Этот README описывает общую архитектуру системы. Для детальной информации о каждом сервисе см. соответствующие README файлы в директориях сервисов.

